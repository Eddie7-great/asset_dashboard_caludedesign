{
  "name": "asset-dashboard",
  "version": "1.0.0",
  "scripts": {
    "test": "node scripts/tests/run_all.mjs",
    "typecheck": "tsc --noEmit",
    "check:tax-rules": "node scripts/check-tax-rule-sources.mjs --offline",
    "check:tax-rules:remote": "node scripts/check-tax-rule-sources.mjs --network"
  },
  "dependencies": {
    "axios": "^1.20.0",
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
